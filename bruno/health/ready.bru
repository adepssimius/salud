meta {
  name: Readiness
  type: http
  seq: 2
}

get {
  url: {{protocol}}://{{host}}:{{port}}/{{apiPrefix}}health/ready
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.status: eq ready
}

settings {
  encodeUrl: true
  timeout: 0
}

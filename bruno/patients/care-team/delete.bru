meta {
  name: Remove Caregiver
  type: http
  seq: 3
}

delete {
  url: {{protocol}}://{{host}}:{{port}}/{{apiPrefix}}patients/{{patientId}}/care-team/{{caregiverUserId}}
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

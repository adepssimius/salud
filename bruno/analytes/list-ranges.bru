meta {
  name: List Reference Ranges
  type: http
  seq: 8
}

get {
  url: {{protocol}}://{{host}}:{{port}}/{{apiPrefix}}analytes/{{analyteId}}/reference-ranges
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Newest effectiveFrom first. There is no effectiveTo — each row ends where the next begins.
}
